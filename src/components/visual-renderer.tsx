/**
 * Geriye dönük uyumluluk kabuğu.
 *
 * Gerçek uygulama `src/visuals/` altına taşındı; orası hem Next uygulaması
 * hem de plugin'in bağımsız viewer paketi tarafından derlenen tek kaynaktır.
 * Yeni kod doğrudan `@/visuals` üzerinden almalıdır.
 */
export { VisualRenderer } from "@/visuals/visual-renderer";
